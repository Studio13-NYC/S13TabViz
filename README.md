# S13TabViz

Visual guitar-tab playback prototype for Studio13 course material.

The working app lives in `Code/`. Open `Code/index.html` in a browser to run the current static prototype.

Versioned course assets include the guidebook PDFs, Guitar Pro source file, backing-track MP3s, and extracted note data used by the prototype.
